# Этап 5. Чемпион Колизея

Финальная практика. У нас уже есть бой, осталось дать ему начало, продолжение и честный финал.

**Основа:** четвёртый этап. Результат — запускаемый `Main.java`, в котором игрок вводит имя, проходит до трёх боёв и получает статистику. Используй правила предыдущей главы без дополнительных механик до первой рабочей версии.

## Собери по шагам

1. Вынеси расчёт урона и лечения в методы. Убедись, что одиночный бой работает как раньше.
2. Вынеси генератор `randomBetween`. Не меняй диапазоны при переносе.
3. Напиши `readAction`: повторяй запрос до команды `0`, `1`, `2` или `3`; при конце ввода верни `"0"`.
4. Перед боем прочитай имя. Если ввода нет уже на этом шаге, сообщи, что турнир не начат, закрой `Scanner` и заверши `main` через `return;`.
5. Добавь внешний `for` на три боя. Здоровье героя, зелья, `wins`, `turns` и `quit` оставь снаружи.
6. Перед внутренним циклом вычисляй характеристики нового врага.
7. После внутреннего цикла сначала проверь выход или смерть. Только затем увеличивай `wins`.
8. Отдых добавь только после первой и второй победы. В конце напечатай итог и закрой ввод.

## Короткий контракт методов

| Метод | Пример проверки |
|---|---|
| `damageAfterArmor(18, 5)` | Возвращает 13 |
| `damageAfterArmor(2, 9)` | Возвращает 1 |
| `randomBetween(12, 18)` | Результат от 12 до 18 включительно |
| `randomBetween(5, 5)` | Всегда 5 |
| `restoreHealth(95, 20, 100)` | Возвращает 100 |
| `readAction(scanner)` | Возвращает допустимую строку; конец ввода означает `"0"` |

Весь код остаётся в одном `Main.java`. Не нужно создавать классы бойцов, список врагов, массив инвентаря или потоки. До них мы доберёмся, когда они будут решать реальную задачу следующего курса.

## Приёмка игры

| Сценарий | Ожидание |
|---|---|
| Имя `Рокки`, затем `0` | Выход, 0 побед, 0 ходов, 100 HP |
| Имя из пробелов | Используется имя `Спартак` |
| Неизвестная команда, затем `0` | Ошибка ввода не меняет состояние |
| Победа в первом бою | Одна победа, отдых до +25 HP, начинается второй бой |
| Выход во втором бою | Одна победа, третьего боя нет |
| Лечение в разных боях | Всего доступно два зелья, запас не обновляется |
| Все три врага побеждены | 3 победы, титул чемпиона, нет четвёртого боя |
| Здоровье героя стало нулевым | Поражение, следующих боёв нет |
| Ввод закончился посреди боя | Выход, незавершённый бой не засчитан |

Для точного контрольного прогона временно сделай так, чтобы `randomBetween(min, max)` возвращал `min`. Введи имя и 16 раз команду `1`. Тогда бои займут 4, 5 и 7 ходов; после отдыха здоровье будет 100, затем снова 100, после финала — 64. Итог: 3 победы, 16 ходов, 2 зелья. **После проверки верни случайность.**

{collapsible="true"}
Подсказка: почему запускается новый бой после выхода
{/collapsible}

`break` внутри `while` вышел только из боя. Сразу после `while`, всё ещё внутри `for`, добавь проверку `quit || heroHealth == 0` и отдельный `break`. Увеличение побед и отдых должны идти после этой проверки.

{/collapsible}

## Как пользоваться полным решением

Вначале сравни порядок действий, потом отдельные формулы. Не обязательно совпадать с эталоном строка в строку: важны поведение и понятность. Затем закрой решение и самостоятельно объясни, где сохраняются зелья и почему защита сбрасывается.

**Готово, когда:** все обязательные сценарии проходят, ты можешь сыграть турнир и объяснить основные части программы. Сохрани этап 5. Это уже законченный небольшой проект, а не обещание когда-нибудь написать игру.


{collapsible="true"}
Разбор и один из вариантов решения
{/collapsible}

```java
import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        System.out.println("Как зовут гладиатора?");
        if (!scanner.hasNextLine()) {
            System.out.println("Ввод завершён. Турнир не начат.");
            scanner.close();
            return;
        }
        String heroName = scanner.nextLine().trim();
        if (heroName.isEmpty()) {
            heroName = "Спартак";
        }

        final int MAX_HEALTH = 100;
        final int TOTAL_FIGHTS = 3;
        int heroHealth = MAX_HEALTH;
        int heroArmor = 2;
        int potions = 2;
        int wins = 0;
        int turns = 0;
        boolean quit = false;

        for (int fight = 1; fight <= TOTAL_FIGHTS; fight++) {
            int enemyHealth = 30 + fight * 10;
            int enemyArmor = fight;
            System.out.println("Бой: " + fight);

            while (heroHealth > 0 && enemyHealth > 0) {
                System.out.println("Герой: " + heroHealth + ", противник: " + enemyHealth
                        + ", зелий: " + potions);
                String action = readAction(scanner);
                boolean guarding = false;
                int attack = 0;

                if (action.equals("0")) {
                    quit = true;
                    break;
                } else if (action.equals("1")) {
                    attack = randomBetween(12, 18);
                } else if (action.equals("2")) {
                    attack = 6;
                    guarding = true;
                } else if (action.equals("3")) {
                    if (potions == 0 || heroHealth == MAX_HEALTH) {
                        System.out.println("Лечение недоступно. Ход не потрачен.");
                        continue;
                    }
                    heroHealth = restoreHealth(heroHealth, 20, MAX_HEALTH);
                    potions--;
                    System.out.println("Здоровье после лечения: " + heroHealth);
                }

                turns++;
                if (attack > 0) {
                    int damage = damageAfterArmor(attack, enemyArmor);
                    enemyHealth = Math.max(0, enemyHealth - damage);
                    System.out.println("Урон героя: " + damage);
                    System.out.println("Здоровье противника: " + enemyHealth);
                }
                if (enemyHealth == 0) {
                    break;
                }

                int defense = heroArmor;
                if (guarding) {
                    defense = defense + 6;
                }
                int enemyDamage = damageAfterArmor(randomBetween(8, 12), defense);
                heroHealth = Math.max(0, heroHealth - enemyDamage);
                System.out.println("Урон противника: " + enemyDamage);
            }

            if (quit || heroHealth == 0) {
                break;
            }
            wins++;
            System.out.println("Победа в бою: " + fight);
            if (fight < TOTAL_FIGHTS) {
                heroHealth = restoreHealth(heroHealth, 25, MAX_HEALTH);
                System.out.println("Здоровье после отдыха: " + heroHealth);
            }
        }

        if (quit) {
            System.out.println("Выход из турнира.");
        } else if (wins == TOTAL_FIGHTS) {
            System.out.println(heroName + " — чемпион Колизея!");
        } else {
            System.out.println("Поражение. Арена подождёт реванша.");
        }
        System.out.println("Побед: " + wins);
        System.out.println("Ходов: " + turns);
        System.out.println("Итоговое здоровье: " + heroHealth);
        System.out.println("Осталось зелий: " + potions);
        scanner.close();
    }

    static int damageAfterArmor(int attack, int armor) {
        return Math.max(1, attack - armor);
    }

    // Контракт курса: небольшие неотрицательные границы, min <= max.
    static int randomBetween(int min, int max) {
        return min + (int) (Math.random() * (max - min + 1));
    }

    // Контракт курса: 0 <= health <= maxHealth, amount >= 0; небольшие числа.
    static int restoreHealth(int health, int amount, int maxHealth) {
        return Math.min(maxHealth, health + amount);
    }

    static String readAction(Scanner scanner) {
        while (true) {
            System.out.println("1 — атака, 2 — осторожный удар, 3 — лечение, 0 — выход");
            if (!scanner.hasNextLine()) {
                return "0";
            }
            String action = scanner.nextLine().trim();
            if (action.equals("0") || action.equals("1")
                    || action.equals("2") || action.equals("3")) {
                return action;
            }
            System.out.println("Нет такой команды. Ход не потрачен.");
        }
    }
}
```

{/collapsible}

---

[← Назад: Правила турнира](/university/%D0%9A%D0%BE%D0%BB%D0%B8%D0%B7%D0%B5%D0%B9/23_%D0%9F%D1%80%D0%BE%D0%B5%D0%BA%D1%82%D0%B8%D1%80%D1%83%D0%B5%D0%BC%20%D1%82%D1%83%D1%80%D0%BD%D0%B8%D1%80) · [Дальше →: Финиш и следующий шаг](/university/%D0%9A%D0%BE%D0%BB%D0%B8%D0%B7%D0%B5%D0%B9/25_%D0%A4%D0%B8%D0%BD%D0%B8%D1%88%20%D0%B8%20%D1%87%D1%82%D0%BE%20%D0%B4%D0%B0%D0%BB%D1%8C%D1%88%D0%B5)
